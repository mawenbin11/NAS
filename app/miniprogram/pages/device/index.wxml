<view class="page">
  <view class="section">
    <view class="title">添加电脑</view>
    <input class="input" value="{{name}}" placeholder="电脑名称" bindinput="onNameInput" />
    <input class="input" value="{{baseUrl}}" placeholder="例如 192.168.1.10:48731" bindinput="onBaseUrlInput" />
    <button class="primary-button" bindtap="onAddDevice">添加并设为当前电脑</button>
    <view wx:if="{{error}}" class="error">{{error}}</view>
  </view>

  <view class="section">
    <view class="title">电脑列表</view>
    <view wx:if="{{devices.length === 0}}" class="empty">暂无电脑，请先添加。</view>
    <view wx:for="{{devices}}" wx:key="id" class="device-card" data-id="{{item.id}}" bindtap="onOpenDevice">
      <view class="device-name">{{item.name}}</view>
      <view class="meta">{{item.baseUrl}}</view>
      <view class="meta">保存文件夹：{{item.targetFolder}}</view>
      <view wx:if="{{item.id === currentDeviceId}}" class="current-badge">当前电脑</view>
    </view>
  </view>
</view>
